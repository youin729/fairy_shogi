doctype html
html(lang='ja')
  include _head.pug
  body
    include _sidebar.pug
    main.one-column
      .card
        h2 新規会員登録
        form(method="POST" action="/signup")
          .form-group
            label ユーザー名
            input(type="text" name="name" class="line")
          .form-group
            label メールアドレス
            input(type="email" name="email" class="line")
          .form-group
            label パスワード
            input(type="password" name="password" class="line")
          .form-button
            button(type="submit" class="btn btn-primary") 会員登録
